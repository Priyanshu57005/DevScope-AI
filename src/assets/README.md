# Assets

Place your custom logo here as `logo.png` or `logo.svg`.

Then in `src/App.jsx`, replace the inline SVG in the Header with:

```jsx
<img src="/src/assets/logo.png" alt="Logo" className="w-8 h-8 rounded-lg" />
```
